# 多 Agent 协作总约束

本文件是本仓库所有 Agent 的共同协作契约。专属入口文件为 `CLAUDE.md`、`TRAE.md` 与 `CODEX.md`；如专属约束与本文件冲突，以本文件为准。

## 1. 决策权与职责

| 角色 | 主要职责 | 不应擅自决定 |
| --- | --- | --- |
| Codex | 项目规划、路线图、架构边界、规格维护、Linear 任务拆分、跨模块验收 | 大规模工程实现，除非用户明确委派 |
| Claude Code | 主要工程实现者：按已批准任务交付应用、共享包、测试与工程配置 | 改变架构边界、协议语义、产品范围或优先级 |
| Trae Work | 辅助工程实现者：承担明确且可独立验证的子任务、测试、文档或配置 | 与 Claude Code 并发改动同一职责域，或自行扩展任务范围 |
| 用户 | 产品目标、优先级、范围、发布与外部系统写入的最终决策者 | — |

## 2. 权威来源与实现边界

- 产品意图：`德州扑克项目总规划.md`。
- 工程规格：`docs/README.md` 中索引的规格文档。
- 分支与 Linear 任务命名：`CONTRIBUTING.md`。
- 纯扑克规则：`packages/poker-engine`；不得依赖数据库、网络、UI 或框架。
- 通信 Schema 与推导类型：`packages/protocol`；不得在客户端和服务端维护平行 DTO。
- `apps/game-server` 负责运行时编排和基础设施适配，不得重写引擎规则。
- `apps/web` 只使用 `src/app` 作为路由入口；客户端不得裁决牌局结果。

未被现有文档定义的重大技术决策，先由 Codex 提出 ADR 或更新规格，再开始实现。不得用代码隐式改变规格。

## 3. 工作流程

1. 从对应的 Linear 任务开始，确认目标、验收条件和关联规格。
2. Linear 任务标题使用 `[TEX-<number>] <可读摘要>`；Git 分支使用 `<type>/TEX-<number>-<kebab-case-summary>`。两者的 `TEX-<number>` 必须一致，但标题不得以分支名替代。
3. 先阅读目标目录的 `README.md`、相关规格与当前代码；只修改任务范围内的文件。
4. 实现时保持模块边界，新增公开接口、事件或持久化结构必须同步更新权威文档。
5. 运行与改动风险相称的测试；扑克规则、边池、全下、重连和协议序列变化必须有针对性验证。
6. 交付时报告：改动范围、验证命令与结果、未覆盖风险、待 Codex/用户决策项。

## 4. 并发与变更纪律

- Claude Code 与 Trae Work 开工前必须声明自己负责的 Linear 任务和目录范围；同一时间不得编辑同一文件或同一模块的核心边界。
- 不修改其他 Agent 已产生但未交接的工作；发现冲突先停止并请求协调。
- 不直接提交到 `main`，不执行破坏性 Git 操作，不提交密钥、真实用户数据或构建产物。
- 不为“以后可能需要”创建空模块、空包、空脚本目录或无验收价值的抽象。
- 任务外的缺陷仅记录为后续建议或 Linear 任务；不得捎带重构。

## 5. 质量与安全底线

- 所有来自客户端的数据均不可信，服务端必须校验并以服务端状态为准。
- WebSocket 变更命令必须可追踪：所有命令使用 `requestId`，`SUBMIT_ACTION` 另使用 `actionId` 与 `expectedSequence`；事件必须具备版本、`tournamentId`、`handId` 与单调序列语义。
- 随机性通过可注入接口获取；生产环境使用安全随机源，测试使用可复现随机源。
- 当前只按模拟筹码范围设计。任何真钱、充值、提现或价值转移能力必须先经用户明确批准、合规与安全审查。

## 6. Pull Request 审查意见闭环

- 无论审查意见来自人工审阅者、GitHub 自动化还是 CodeRabbit 等审查机器人，只要任务包含对该意见的修改，必须完成对应修改、通过适用验证、提交并推送到该 PR 的源分支后，再在**对应的原始审查评论线程**回复：`已修正`。
- 不得在修改尚未推送、回复了错误评论线程，或实际未完成对应修改时使用“已修正”。若确认某条意见无需修改，必须在该评论线程说明依据，不得以“已修正”代替说明。
- 交付摘要应列明已处理的审查意见及其评论回复完成情况；未完成对应线程回复，不得将审查修改任务标记为完成。

## 7. DeepSeek Harness 审查的启动方式

- 开发完成后的 DeepSeek Harness 审查由用户手动启动。Claude Code、Trae Work 与 Codex 不得自行调用该审查 Agent，也不得将 DeepSeek Harness 审查作为提交、推送或创建 PR 的前置门禁。
- 用户提供 DeepSeek Harness 的审查结果后，相关 Agent 应将其作为 PR 审查意见处理：完成适用修改与验证、提交并推送，然后按本文件的审查意见闭环规则回复对应评论。

## 8. 文档同步与冲突处理

仓库文档是持续维护的交付物，不是初始化时的一次性说明。任何 Agent 在开发过程中触及某个目录、模块或运行流程时，必须同步维护其**对应位置**的文档，确保代码、任务和文档描述同一事实。

1. 新增、删除或改变某目录的职责、公开接口、运行方式、配置方式、测试资产或所有权时，必须同步更新该目录的 `README.md`。如果该目录已成为稳定职责边界但尚无 README，可创建一份说明用途、边界、主要入口、运行/测试方式的 README；不得为尚未承担职责的空目录预建 README。
2. 发生产品范围、用户流程或阶段任务变化时，更新 `docs/00-project/` 或 `docs/01-product/`；发生模块边界、数据流或重大技术决策变化时，更新 `docs/02-architecture/`、相关权威规格或 ADR；发生开发流程、测试、CI 或工具变化时，更新 `docs/03-engineering/`；发生安全、权限、秘密、投影或审查要求变化时，更新 `docs/04-security/`；发生部署、监控、发布、回滚或事故处理变化时，更新 `docs/05-operations/`。
3. `docs/01-engine-spec.md` 至 `docs/06-testing-strategy.md` 是对应工程事实的权威规格。不得在其他位置复制并逐渐漂移；其他文档应链接引用。若两份文档矛盾，先停止相关实现，确定权威来源后在同一任务中修正冲突。
4. 每个任务完成前必须检查：本任务涉及的目录 README、权威规格、任务卡/路线图、测试说明和运维/安全说明是否需要更新；若无需更新，交付摘要必须明确写出“已检查，无需更新”及原因。
5. 只有文档更新与代码/配置变更同一 PR 交付，并且索引链接有效、内容未与权威规格冲突，任务才可标记完成。文档维护本身也必须遵循对应 Linear 任务和分支规则。

## 9. 完成定义

任务只有在以下条件满足时才可标记完成：验收条件满足、相关测试通过、目录 README 与相关文档/接口同步、没有未说明的范围变更，并已给出可审查的交付摘要；如任务处理了 PR 审查意见，对应评论线程也已在推送后完成回复。
