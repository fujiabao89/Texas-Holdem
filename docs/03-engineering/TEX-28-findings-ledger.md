# TEX-28 审查 Findings Ledger

审查范围：`test/TEX-28-implement-p0-integration-e2e-and-security-tests`。本轮为 Trae Work 工程执行中主动发现的生产缺陷与依赖核验记录；未触发 Greptile 或 DeepSeek Harness。

| ID | 来源 / 核验 | 有效性、精确失败场景与现有覆盖 | 严重度 | 处置 |
| --- | --- | --- | --- | --- |
| F-1 | 进程内全链路 harness 冒烟（tests/clients/server-harness.test.ts）实测发现 | 有效。`TournamentStarter.start()` 在 Room 队列 `persist()` 内调用 `manager.create()`，其内部 `executor.submit({type:"START"})` 以微任务排队，先于 Room 执行器提交 `this.state = next` 执行；首手事件（HAND_STARTED/盲注/发牌，实测 sequence 1..7）流经事件总线时 `manager.getSnapshot(roomId).activeTournamentId` 仍为 null，被 lobby-gateway 的房间过滤丢弃。后果：跨开局边界保持连接且不重新认证的客户端永远收不到首手事件，也不满足 docs/02 §8.2「GAME_SNAPSHOT 首次进入」的推送语义。现有 E2E 全部使用 `page.routeWebSocket` mock（tests/e2e/betting/table.spec.ts 等），从未覆盖真实链路，故未暴露。当前 web 端主流程经页面导航触发 disconnect+重连（poker-table-page 亦挂 useLobbyConnection）获得 RECONNECT_RESULT.gameSnapshot 自愈；「再局」时停留在牌桌页不导航的客户端存在卡死风险。复现证据：harness 冒烟测试中 ALICE socket 收到的消息仅 RECONNECT_RESULT/ROOM_SNAPSHOT/COMMAND_RESULT，无任何 GAME_EVENT，而 tournamentManager.getView 显示 RUNNING、lastWireSequence=7。 | P1（已修复，见 F-7） | TEX-28 收尾：用户裁决采纳「源头修复」并在本分支落地（F-7），跨开局边界保持连接的开局前 socket 现可收到完整首手事件；未单开 Linear。 |
| F-2 | 依赖核验（TEX-36） | 有效。origin/main 仍缺失 GET /api/v1/tournaments/{tournamentId}/hands 与 GET /api/v1/tournaments/{tournamentId}/hands/{handId} 两个 Hand History 读取端点（docs/02 §8.1/05 §13 已定义）。归档历史的真实联调与授权隔离验收被阻塞。 | P1（既有已知） | 不在 TEX-28 偷偷实现；相关真实链路不宣称通过。归档历史 UI 侧已有 TEX-27 Fake E2E 覆盖；服务端补齐后需另做端到端验收。是否豁免由用户决定。 |
| F-3 | 真实链路多人旅程 E2E（tests/e2e/real/multiplayer-journey.spec.ts）实测发现 | 有效。锦标赛终局后 `finishTournament` 将房间迁移为 `FINISHED + activeTournamentId: null`（room-runtime.ts），此后任何 WS 认证的 `RECONNECT_RESULT.gameSnapshot` 恒为 null（lobby-gateway.ts）。客户端「牌桌 → 查看比赛结果」的页内导航会重挂 `useLobbyConnection`（disconnect+reconnect），`acceptReconnectResult(room, null)` 清空 store 中唯一的终局快照，结果页永远落入「快照不可用」——TEX-27 交付的结果页在真实链路下从未可达（其 Fake E2E 未覆盖该页面）。后果：核心用户旅程「赛果查看」断裂；刷新/直达结果 URL 同样不可用（依赖服务端结果查询端点，与 F-2 同根）。 | P1 | 双轨处置：(1) 客户端修复已在本分支落地——`acceptReconnectResult` 对同房间已 `FINISHED` 的终局快照予以保留（终局不可变，新一局开始时 GAME_SNAPSHOT 屏障整体替换；换房间不保留），配 projection-store.test.ts 回归；(2) 刷新/直达场景仍需服务端结果数据源（TEX-36 Hand History 端点或等价投影），架构取向（终局快照保留时长 / 专用结果端点）留待 Codex 裁决后另开任务。 |
| F-4 | 进程内复现脚本（scripts-tmp/repro-inprocess.ts）+ 真实链路 E2E 实测发现 | 有效。三人同手全下双淘汰时，`buildHandCommitBundle` 对组内所有 `PLAYER_ELIMINATED` 均写入 `rank: placementRange.from`，撞 `tournament_players_tournament_rank_unique` 部分唯一索引（23505），整手 Bundle 回滚，锦标赛 `last_committed_sequence` 停滞、水位不推进、结果页/再来一局全部卡死。修复：组内按引擎 `displayOrder`（手开始筹码多者优先）打破并列，`rank = placementRange.from + displayOrder - 1`，与 docs/03 §5.4 rank 唯一性一致；新增 tournament-persistence.test.ts 回归（组内 rank 唯一、冠军 rank=1）。 | P1（已修复） | 已在本分支修复并通过进程内复现（hand#1/#2 均提交、锦标赛 FINISHED、水位=事件数）与真实链路 E2E 验证。 |
| F-5 | axe-core 扫描（tests/e2e/real/accessibility.spec.ts）实测发现 | 有效。牌桌卡片组件 `CardFace`/`CardBack`/`BoardDealCard` 以无 role 的 `<span aria-label>` 呈现牌面（「未公开牌」「方块 10」等），axe `aria-prohibited-attr` 判 serious（WCAG 4.1.2）。修复：三处加 `role="img"`（牌面为纯视觉元素，语义正当）。 | P3（已修复） | 已在本分支修复；serious 门槛恢复为零。 |
| F-6 | 真实链路 E2E（chromium）实测发现 | 有效。lobby-page.tsx 渲染期调用 `tokens.get(roomId)`（内部读 sessionStorage），SSR 输出 `aria-live={null}`/缺省分支与客户端 `aria-live="polite"` 不一致，触发 hydration mismatch（poker-table/result 已用 `useSyncExternalStore(subscribeNever, …)` 处理，lobby 缺失）。修复：照搬 `isBrowser` 门——非浏览器渲染 loading 分支，`isBrowser` 后才读 sessionStorage。 | P2（已修复） | 已在本分支修复；hydration 相关真实链路（chromium/firefox/webkit）通过。 |
| F-7 | F-1 同源服务端竞态（根因 A）的源头修复与复证（TEX-28 收尾） | 有效（复证根因 A）。真实链路 debug 帧级实证：开局前已连接的大厅 socket 仅收到 PLAYER_ALL_IN 起的事件，缺失 HAND_STARTED/BLIND_POSTED/DEAL_HOLE_CARD。机制：原 `createRuntimeTournamentStarter.start()` 在 Room 队列 `persist()` 内同时落库并 `manager.create()`（→ `executor.submit(START)` 微任务 drain 产出首手事件），早于 `RoomRuntime.process` 的 `this.state = next`（IN_GAME 提交）；lobby-gateway 按 room 快照 `activeTournamentId` 过滤时首手事件被静默丢弃。源头修复：把控制面落库与运行时注册解耦——`createPersistenceTournamentStarter`（提交前落库）+ `createRuntimeTournamentRegistrar`（仅 `manager.create`）；`RoomRuntime.process` 在 `state = next` 提交之后经 `onStartCommitted` 同步注册并驱动 START，使首手事件恒晚于含 `activeTournamentId` 的 room 快照。回归：room-executor F-7 单测（注册回调触发时 `status=IN_GAME && activeTournamentId=本局`）+ 真实链路 chromium 开局前 socket 收到 4×DEAL_HOLE_CARD。 | P1（已修复） | 本分支修复（main.ts/server-harness/tournament-manager.test 同步接线）；真实链路三浏览器全量 15/15 通过。 |
固定项将在验证、提交和推送后仅回复对应原评论线程“已修正”。跳过项将在原线程说明依据，不以“已修正”代替。
