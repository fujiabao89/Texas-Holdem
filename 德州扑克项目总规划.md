# Texas Hold'em 项目总规划

> 版本：v1.0（已合并区块 1–10 与本轮决策）  
> 日期：2026-08-20  
> 目标：先交付可稳定完成整场真人私人牌局的 P0；在不重写规则、协议或房间架构的前提下，于 P1 扩展单人和 AI。

## 1. 产品定义与阶段边界

本项目是一款极简、白色主体、响应式的 No-Limit Texas Hold'em Web 游戏。核心价值是：标准规则正确、朋友可快速开设并完成一场私人比赛、牌桌操作顺畅。

| 阶段 | 范围 | 明确不做 |
| --- | --- | --- |
| P0 | 自定义私人房：2–10 名真人、邀请码加入、Lobby、选座、Ready、完整单桌 Freezeout、实时同步、重连、基础动画与音效 | 单人模式、AI/DeepSeek、外部观战、账号体系、聊天、匹配、商业化 |
| P1 | 单人模式（1 真人 + 1–9 AI）；好友房中的 AI 座位与真人/AI 混合牌局；DeepSeek 困难 AI、本地 Fallback、AI 记忆与成本保护 | 多模型、多难度、用户 API Key、GTO Solver |
| 后续 | 账号、战绩、Replay、更多预设和 AI 难度；Public Room、匹配、MTT 等 | 真钱、充值、商城、KYC 不在当前路线图 |

P0 并不是一次性原型。它从第一天就采用通用的 `Participant`、`ActionSource`、`TournamentConfig`、`PlayerView` 和事件协议；P1 只是在现有座位中加入 `BOT` 参与者，并让 AI Controller 像真人客户端一样向 Engine 提交 Action。Poker Engine、Pot、房间、重连和 UI 布局均不因 AI 而改写。

## 2. 已确认的产品与规则决策

### 2.1 参赛与排序

- 牌桌为 2–10 个稳定 Seat；空位、真人、未来 AI、淘汰者都具有固定 `seatIndex`。
- 首手 Dealer 从有效 Seat 中用服务器随机源选择；后续 Dealer 按顺时针寻找下一名仍在比赛中的 Seat，跳过空位、退出者和淘汰者。
- 同手多人淘汰时，以该手**开始时**的筹码较多者排名更高；开始筹码相同则使用 `seatIndex` 升序作为稳定展示顺序。此排序只解决并列展示，不伪造扑克规则上的先后淘汰。
- P0 开局条件：至少 2 名真人参赛者；所有真人（包括房主）均处于 Ready；无未完成的配置变更；房间仍在 `LOBBY`。房主不能绕过自己的 Ready 状态强制开始。
- P1 加入 AI 后，仍要求至少 2 名总参赛者，且至少 1 名真人；AI 默认 Ready。单人模式固定为 1 真人 + 1–9 AI。

### 2.2 标准扑克规则

- 标准 52 张牌、无 Joker、No-Limit Texas Hold'em；每人两张底牌、最多五张公共牌。
- 完整流程：`HAND_START → POST_BLINDS → DEAL_HOLE_CARDS → PREFLOP → BURN/FLOP → BURN/TURN → BURN/RIVER → SHOWDOWN → POT_SETTLEMENT → HAND_END`。
- 支持 Fold、Check、Call、Bet、Raise、All-in、Main Pot、任意数量 Side Pot、Split Pot 与 Odd Chip。
- Short All-in 合法，但不足完整最小加注时通常不重开已行动玩家的 Raise 权。Engine 维护 `currentBet` 和 `lastFullRaiseSize`，并直接输出当前玩家的 `LegalActions`。
- 允许最佳五张完全来自公共牌；A-2-3-4-5 是 5-high Straight；Fold 玩家已投入筹码保留在 Pot，但不再有获奖资格。
- Odd Chip 从 Dealer 左侧开始顺时针，给第一个符合该 Pot 获奖资格的赢家。
- 采用 Freezeout。筹码在当前 Hand 结算后仍为 0 才正式淘汰；仅剩一名有效参与者即产生冠军。

### 2.3 可配置比赛结构

房主只可在 Lobby 调整比赛结构，不能修改核心扑克规则。配置至少包括最大人数、初始筹码、SB/BB、固定/按时间升级/按手数升级盲注、盲注层级、行动时间和 Time Bank。

- SB、BB 必须为正整数且 `SB < BB`；初始筹码和层级数也必须为受限正整数。
- **允许盲注下降**。每个 Blind Level 独立校验即可，不要求后一层大于等于前一层。
- 盲注变更（无论上升或下降）只在当前 Hand 完整结束后生效；不会在一手牌中途改变下注下限或盲注。
- 每手开始时按当前 BB 初始化该手的最小下注/完整加注基准，因此下降盲注不继承上一手的最小加注幅度。
- 比赛开始后锁定初始筹码、盲注结构、参与者和 Seat，不新增真人/AI、不换座、不改核心配置。

## 3. 时间、Time Bank 与截止竞争

### 3.1 限时与不限时

| 行动时间设置 | 服务端行为 | Time Bank |
| --- | --- | --- |
| 10–60 秒等有限时 | 服务端为当前行动者建立 `actionDeadline`；超时后 Check 合法则 Auto Check，否则 Auto Fold | 可关闭，或设为 30/60/120 秒等比赛总额外时间 |
| 不限时 | 不创建行动超时任务，不自动 Check/Fold | **强制禁用**，UI 不展示、协议不接受 `USE_TIME_BANK` |

有限时下，玩家主动点击“使用延时”才消耗 Time Bank：一次延长当前行动的固定时段（例如 30 秒）或剩余余额中较小者。断线不会自动消耗 Time Bank。

### 3.2 行动与超时同时到达的裁决

服务端以单桌串行执行器和单调时钟为权威，绝不由客户端倒计时决定。

1. Action 进入服务端即记录不可伪造的 `receivedAt` 与 `actionId`，然后进入该 Tournament 的串行队列。
2. `receivedAt <= actionDeadline` 的合法 Action 优先于同一截止点触发的超时任务；服务端先处理截止点前已接收的 Action，再处理 Timer。
3. `receivedAt > actionDeadline` 或 Action 尚未取得该桌串行执行权时，超时任务获胜；该 Action 返回 `ACTION_TIMEOUT` 或 `STALE_GAME_STATE`，不执行。
4. 同一 `actionId` 最多执行一次；Action 可携带 `expectedSequence`，状态过期则拒绝并下发最新 Snapshot。

这使边界情况可重放、可测试且对网络快慢一致：裁决点是服务端接收与串行化顺序，而非按钮在客户端何时被点击。

## 4. 离开、掉线、重连与房间关闭

### 4.1 状态定义

- 关闭浏览器、网络中断、切换网络或手机进入后台均先视为 `DISCONNECTED`，不是立即离开。
- 断线玩家保留 Seat、筹码和本手已投入筹码；有限时下轮到其行动时按普通超时 Auto Check/Auto Fold；不限时下仍保留 10 分钟断线宽限，不会无限阻塞牌局。
- 断线满 10 分钟，或用户确认“主动离开比赛”，标记为 `EXIT_PENDING`。当前 Hand 如能正常结算则先结算；未 All-in 且仍有行动资格者立即按弃权折叠处理，已投入筹码仍留在相应 Pot。
- 在安全结算点，玩家转为 `WITHDRAWN`：不再获得后续发牌、盲注、行动或排名资格；其未投入的剩余筹码记入 `forfeitedChips`，不会无端赠与其他玩家。筹码守恒不变量因此为“在场筹码 + 未结算 Pot + forfeitedChips = 初始总筹码”。

### 4.2 比赛结果和房间关闭策略

- 比赛的有效竞争者是未 `WITHDRAWN` 的在线真人，以及 P1 中仍在参赛的 AI。主动离开和断线超过 10 分钟的真人不计入最终名次。
- 如果仍有真人，比赛继续：只剩一名真人且没有 AI 时，该真人立即成为冠军；有 AI 时继续按标准比赛规则完成。
- **只要房间中已没有真人**，立刻停止后续 AI 决策与计时任务，房间转为 `CLOSED`，比赛记录为 `ABANDONED_NO_HUMAN`，不把 AI 自动宣告为冠军。已完成的 Hand History 与断开原因持久化保存。
- `CLOSED` 房间的邀请码立即失效，不能再加入或“再来一局”。有真人完成比赛后才进入 `FINISHED → LOBBY`，可保留邀请码与配置开始下一场。
- 比赛前房主离开，房主权转给最早加入且仍在线的真人；比赛中房主断线不影响服务端运行，也没有任何特殊扑克权限。

## 5. 房间、联机与协议

### 5.1 房间生命周期

`CREATED → LOBBY → IN_GAME → FINISHED → LOBBY`；在 Lobby 或无真人时可转为 `CLOSED`。Room 是邀请和人员容器，Tournament 是 Room 中的一次实际比赛；“再来一局”创建新 Tournament，不复用旧牌局状态。

P0 首页只有两个核心入口：创建房间、加入房间。邀请码是 6 位不易混淆的大写字母/数字组合，只定位房间，不充当身份凭证；邀请链接仅预填邀请码。

### 5.2 身份与实时同步

- 无账号版本使用 `playerId + 高熵 playerToken + roomId` 识别玩家；昵称只用于展示，同房间不可重复。
- HTTP 处理创建、加入和低频设置；WebSocket 处理 Lobby 更新、Ready、牌局 Event、Action 与重连。
- 服务端维护 `connection → player → seat` 映射。客户端只能用自身身份提交动作，不能指定某个 Seat 操作。
- 使用 `Snapshot + 有序 Event Stream`：进入/重连先获得合法的 Player Snapshot，之后消费带 `sequence` 的 Event；漏序、积压或过期状态直接重取 Snapshot。断线期间不重放旧动画。
- 一张 Tournament 桌的所有状态改动、Timer 和未来 AI Action 均走同一串行队列；服务端是唯一权威状态源。

### 5.3 私有信息投影

内部 `GameState` 绝不直接发送浏览器。服务端按接收者生成 `PlayerView`：仅本人可看自己的底牌；其他未公开底牌、Deck 顺序、Burn Card 牌面和未来 AI 的内部 Context 均不会出现在未授权 Payload。淘汰观战者仅看公开信息；P0 不支持外部自由观战。

## 6. P0 工程架构与可扩展性

采用与 [Wolfcha](https://github.com/oil-oil/wolfcha) 同路线的“Next.js 前端 + Supabase 数据层”，但不照搬其狼人杀业务后端。项目使用 pnpm workspace 管理单仓库：Next.js 16 + React 19 + TypeScript + Tailwind CSS 4；Jotai 管理客户端 UI 状态；Radix UI 提供无障碍基础组件；Framer Motion 实现牌桌动效。该组合与 Wolfcha 当前公开依赖保持一致，降低团队的学习和维护成本。

扑克仍需要独立、常驻的实时游戏服务：Node.js + Fastify + `@fastify/websocket` 负责 Room/Tournament、单桌串行队列、服务端计时、断线恢复、State Projection 和实时 Event；不能把权威牌局状态交给浏览器、普通 Next Route Handler 或数据库订阅。Supabase Postgres 保存 Room、Tournament、Hand History、Snapshot、结果与 P1 的 AI 用量；P0 沿用自定义 `playerId + playerToken`，不为此提前引入 Supabase Auth。

```text
poker/
├─ apps/web/                 # Next.js：React UI、Seat Layout、AnimationQueue
├─ apps/game-server/         # Fastify：Room/Tournament/Scheduler/WebSocket/Persistence
├─ packages/poker-engine/    # 纯规则：不得依赖 UI、网络、数据库、AI
├─ packages/poker-math/      # P1 的 Equity、Pot Odds、Position 等确定性计算
├─ packages/protocol/        # Action/Event/Snapshot/ErrorCode 与 Schema
├─ packages/shared/
├─ tests/
└─ docs/
```

关键边界：规则属于 Engine；唯一真实状态属于 Game Server；客户端只能展示和提交请求；动画只能消费已确定 Event；P1 的 DeepSeek 只可选择 Engine 给出的合法 Action，不能直接修改筹码、牌、Pot 或 Dealer。

P0 即预留：`participant.kind = HUMAN | BOT`、`ActionSource = HUMAN_SOCKET | BOT_CONTROLLER | SYSTEM_TIMER`、统一 `TournamentConfig`、投影层和 AIProvider 接口。P0 仅启用 `HUMAN` 与 `HUMAN_SOCKET/SYSTEM_TIMER`，不接 API、不显示 AI 控件。

### 6.1 Wolfcha 可复用边界

- 可复用/借鉴：Next + Tailwind + Jotai + Radix + Framer Motion 的前端工程组合；通用 UI 组件的组织方式；Supabase Client/Admin Client 的服务端组织方式；AI Provider 配置、结构化输出校验、调用日志和成本统计的分层思路；按游戏阶段拆分业务模块的结构。
- 必须重写：No-Limit Hold'em Poker Engine、Pot/Side Pot、Hand Evaluator、Tournament、单桌串行状态机、WebSocket 协议、行动超时与断线恢复。Wolfcha 的公开项目仍将好友多人玩法列为路线图，其会话接口主要处理数据库记录，不可作为本项目 P0 的权威实时牌局实现。
- 不纳入本项目：狼人杀角色和阶段代码、Tiptap 聊天、Stripe、OAuth、TTS、赞助/支付逻辑，以及狼人杀主题的角色图、旁白、BGM、Logo 等素材。
- 许可证：只使用相同技术栈不受 Wolfcha 源码许可证约束；若复制其代码或素材，按仓库实际的 Apache-2.0 许可证保留版权、许可证与 NOTICE，并对素材逐项确认来源和授权。规划与实现优先自行编写德州扑克专属代码。

## 7. P0 功能设计

### 7.1 Lobby 与牌桌

- 房主创建房间并配置人数、筹码、盲注、行动时间；高级面板编辑 Blind Structure。
- 玩家通过邀请码和昵称加入，可选空 Seat 或随机入座；Lobby 展示座位、Ready、房间配置和邀请码。
- 房主可在开局前改配置、踢人；所有真人 Ready 且人数达标后可开始。开局后房主只是一名普通参赛者。
- 牌桌适配 2–10 人：自身始终视觉上位于下方中间，其他 Seat 相对旋转；桌面横向布局，手机竖屏重新排布，不是简单缩放。
- 只在轮到自己行动时展示 Fold/Check/Call/Bet/Raise/All-in。常规下注用 BB 倍数、Pot 比例、Slider 和 ± 调整；点击金额才打开精确数字输入。All-in 使用“先选全额、再点击确认”的两步操作。

### 7.2 体验与持久化

- Event 驱动发牌、Burn、Flop/Turn/River、下注、Fold、Showdown 与 Pot 分配动画；慢动画不能阻塞服务端，事件积压可 Fast Forward 到 Snapshot。
- Burn Card 只展示牌背移入弃牌区/淡出，不翻开。
- P0 提供发牌、翻牌、下注、Fold、All-in、获胜和盲注升级基础音效；Showdown 高亮最佳五张牌。
- 每手保存结构化 Event；用户版 Hand History 通过 PlayerView 过滤隐藏信息。每手结束保存 Snapshot，满足刷新、切网和手机后台恢复；服务进程崩溃后的进行中 Hand 无损恢复不作为 P0 发布硬要求。

## 8. P1：单人与 AI

P1 复用 P0 的 Tournament、Seat、协议、行动队列和 PlayerView。

- 单人：1 真人 + 1–9 AI；好友房可在 Lobby 向空 Seat 添加/移除 AI，开局后锁定。
- AI Controller 在服务器运行，和真人 Action 一样进入 Engine；DeepSeek API Key 永不进入前端、WebSocket 或日志。
- 服务器先用 `poker-math` 生成 Poker Context（牌力、估算 Equity、Pot Odds、位置、Stack BB、合法动作、公开行为），LLM 仅在多种合法选择间做策略决策。
- DeepSeek 输出先过 JSON Schema 和 Engine Legal Action 校验；超时、500、无效 JSON 或非法 Action 时立即使用本地 Fallback。只有唯一合法动作时不调用 LLM。
- 每个 Bot 的对手模型与短期历史独立；只能使用公开信息，绝不共享底牌、Deck、其他 Bot Prompt/Response 或内部推理。
- 控制成本：最多 9 AI、限制 Prompt/输出 Token、监控请求数/延迟/Fallback Rate、无真人时立即停止。

## 9. 测试、质量与发布门槛

### 9.1 P0 必测

- Deck 52 张唯一；Dealer/Blind/Heads-Up；完整下注、最小加注、Short All-in、Call All-in、Raise 权重开。
- 普通/多 Side Pot、Fold 贡献、Split Pot、Odd Chip；所有牌型、Kicker、A2345、Board Plays。
- 淘汰、同手多淘汰稳定排序、盲注按手/时间变更（含下降盲注）、不限时、Time Bank、超时竞争裁决。
- 邀请码唯一与限流、昵称重复、Lobby 权限、Ready、开局锁定、Host 转移、主动离开、10 分钟断线退出、无真人关房。
- WebSocket 的重复 `actionId`、缺失 sequence、旧状态 Action、多设备接管、刷新/切网/后台恢复。
- PlayerView 字段级测试：任何非授权 Payload 不含其他底牌、Deck、Burn Card 或服务端私密字段。
- Headless Simulator：用可注入随机 Seed 运行数千场 2–10 人比赛；每个合法 Action 后断言筹码守恒、牌唯一、Pot 非负、Actor 唯一、Fold 玩家不可获奖等不变量。发现异常必须能按 Seed 100% 复现。

### 9.2 分级与上线标准

| 级别 | 示例 | 处理 |
| --- | --- | --- |
| P0 | 错误发牌、重复牌、Pot/赢家错误、私密底牌泄露、比赛死锁 | 禁止上线 |
| P1 | 重连失败、计时/下注额错误、Action 冲突、主流程不可用 | 阻断发布 |
| P2 | 非核心 UI 偏移、轻微掉帧 | 排期修复 |

P0 发布前必须：全部核心规则测试通过；数千场随机比赛无死锁或不变量破坏；至少两台真实设备能通过邀请码完成完整比赛；刷新、断网、网络切换和移动端后台恢复可用；客户端不能读取隐藏信息；手机常规下注无需键盘；核心发牌/翻牌/Showdown 动画在桌面和手机上可接受。

## 10. 实施路线图

1. **Engine**：Cards、Deck、Evaluator、Betting、Pot、Tournament、Event、RNG、Invariants。
2. **规则测试与 Simulator**：先覆盖高风险规则，再做可复现随机长跑。
3. **服务端骨架**：Room、Lobby、Config 校验、持久化、Snapshot/Event、串行队列与 Timer。
4. **P0 联机牌局**：身份/Token、WebSocket、Ready、开局、重连、离开与关房策略。
5. **P0 Web UI**：Home、Create/Join、Lobby、动态 Seat 牌桌、下注交互、响应式。
6. **P0 体验与验收**：AnimationQueue、音效、实机 E2E、稳定性和安全测试，发布 P0。
7. **P1 AI 基础**：Poker Math、BotView、AIProvider、Fallback、AI Scenario 测试。
8. **P1 产品接入**：单人设置、AI Seat、混合房间、成本/故障监控，发布 P1。

## 附录 A：当前不纳入 P0/P1 的内容

陌生人匹配、公共大厅、排位、好友关系、复杂账号、文字/语音聊天、外部自由观战、真金/充值/商城/VIP、多桌 MTT、Rebuy/Add-on/Bounty、用户自带模型/API Key、多 AI 难度、微服务、Redis、Kafka、Kubernetes、多区域部署。

## 附录 B：架构红线

1. 外部模块不得绕过 Engine 直接修改牌局状态。
2. 浏览器不得获得完整内部 GameState，更不得“先发送再用 CSS 隐藏”底牌。
3. 同一 Tournament 的状态变更必须串行；客户端和 AI 都不能并发直接 mutate。
4. 所有外部输入必须校验；所有动作必须再次校验身份、回合、金额与合法性。
5. 局部网络、客户端或 AI 故障只能降级该参与者，不能让整桌失去一致性。
