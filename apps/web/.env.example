# Web 环境变量样例（不含任何密钥）。
# 仅 NEXT_PUBLIC_* 前缀的变量会暴露到浏览器，只放非敏感的公开配置。
# 任何密钥、令牌、数据库凭据都必须留在服务端，绝不以 NEXT_PUBLIC_ 前缀暴露。
# 敏感值只由部署平台注入，绝不写入本文件或提交到仓库。
# NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:3001
# NEXT_PUBLIC_WS_URL=ws://127.0.0.1:3001/api/v1/ws
